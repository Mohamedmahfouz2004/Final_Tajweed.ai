# FastAPI core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Database (MongoDB async)
motor>=3.7.0
pymongo>=4.12.0

# Auth (Clerk) — verify Clerk session JWTs (RS256) via JWKS using python-jose.
# User management uses the Clerk Backend API over httpx (see app/services/clerk.py).
python-jose[cryptography]>=3.3.0

# Validation & Config
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# Email
aiosmtplib>=3.0.0
email-validator>=2.0.0

# File uploads
python-multipart>=0.0.20
aiofiles>=24.0.0

# WebSocket client (for proxying to model server)
websockets>=14.0
httpx>=0.28.0

# Utilities
python-dateutil>=2.9.0
